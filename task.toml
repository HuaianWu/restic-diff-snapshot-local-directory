schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/restic-diff-snapshot-local-directory"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "khf23fefd35462d0d34f47506d268e44"
task_id = "restic-diff-snapshot-local-directory"
display_title = "Let restic diff compare a snapshot with a local directory"
display_description = "When a single snapshot is given, diff it against a local target directory and report added, removed, and modified files by content only."
original_title = "restic diff 支持快照与本地目录对比"
category = "feature_request"
language = "go"
repository_url = "https://github.com/restic/restic"
base_commit_hash = "ba802d42b7294c98b62c16d1157ea3e80820c019"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary ba802d42b7294c98b62c16d1157ea3e80820c019 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:khf23fefd35462d0d34f47506d268e44-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
